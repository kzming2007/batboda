# Design

> 확정: 2026-08-04 본선 Day 1 야간
> 근거: 멘토 피드백(이미현), 문제정의서 요구사항 ④, 브라우저 접근성 감사 실측
> 전제: `PRODUCT.md`의 사용자(초보 귀농인 + 고령층)와 피할 방향(초록 클리셰 / 관공서 표 덩어리)

---

## Theme

밝은 테마 하나. 다크 모드는 두지 않는다.

물리적 장면이 이것을 정한다 — **처음 농사를 시작하려는 사람이 낮에 밭 앞이나 집 책상에서, 폰이나 노트북으로 확인한다.** 야외 햇빛과 고령층 가독성이 겹치므로 밝은 배경과 높은 대비가 필요하다. 어두운 테마는 이 장면에 맞지 않는다.

## Color

**전략은 Restrained.** 브랜드 색 하나와 상태색 넷만 쓴다. 장식으로 쓰는 색은 없다.

### 왜 초록을 버리는가

농사라서 초록을 쓰는 것은 분야만 듣고 짐작되는 첫 반사다. 그 다음 반사인 흙빛 테라코타·황토도 같은 이유로 피한다. **딥 시안은 두 반사 밖에 있고, 상태색 셋과 색조가 충분히 떨어져 혼동이 없다.**

초록은 버리지 않는다. **`기준 안` 상태에만** 남긴다. 상태에만 쓰이는 초록은 초록 일색이 아니다.

### Tokens

```
--pg          #f7f9fa   페이지 배경. 채도 거의 0, 브랜드 색조로만 살짝 기울임
--sf          #ffffff   카드·패널
--ink         #14181a   본문·제목
--ink-soft    #55636a   보조 본문. 배경 대비 5.6:1
--line        #dde4e6   괘선
--line-strong #b9c4c7   강조 괘선

--cy          #0e7490   브랜드 주 색. 주요 버튼 바탕(흰 글자 5.4:1)
--cy-ink      #0b5a72   브랜드 글자색. 링크·현재 선택 낱말
--cy-lt       #06b6d4   강조. 현재 선택 밑줄, 강조 면 바탕
--cy-pale     #cbf1f8   강조 면 바탕(진한 글자 얹음)

--good        #1c6f37   기준 안
--good-bg     #dcecdf
--good-line   #a9cdb4
--watch       #8a5806   주의
--watch-bg    #f7ead2
--watch-line  #e0c592
--bad         #b13327   기준 밖
--bad-bg      #f8e2de
--bad-line    #e6b3a5
--unknown     #55636a   확인 필요 — 경고색을 쓰지 않는다
--unknown-bg  #e6eae7
--unknown-line #c6cec9

--deep        #10262e   확정한 필지 · 최근 관측만 쓰는 어두운 면
--deep-soft   #a7c0c8
--deep-line   #2b4550
--sf-sunk     #f1f4f5   같은 면 안에서 한 단계 들어간 자리
```

**상태색 셋을 2026-08-04에 다시 내렸다.** 이전 값은 자기 배경 위에서 셋 다 4.5:1 미달이었다
— 초록 4.38:1, 황 3.26:1, 적 4.38:1. 배지·태그 글자가 15px이라 큰 글자 기준(3:1)을 쓸 수 없다.
**황은 흰 바탕에서도 3.87:1로 미달이어서 `먼저 할 일`의 시점 글자까지 걸렸다.**

지금 값의 실측 대비다.

| 토큰 | 자기 배경 | 흰 바탕 | 페이지 배경 |
|---|---|---|---|
| `--good` #1c6f37 | 5.06:1 | 6.22:1 | 5.89:1 |
| `--watch` #8a5806 | 5.07:1 | 6.04:1 | 5.72:1 |
| `--bad` #b13327 | 5.02:1 | 6.23:1 | 5.90:1 |
| `--unknown` #55636a | 5.12:1 | — | 5.88:1 |
| `--cy-ink` on `--cy-pale` | 6.43:1 | — | — |
| 흰 글자 on `--cy` | 5.36:1 | — | — |

### 지켜야 할 것

**밝은 시안 `#06b6d4`에 흰 글자를 얹지 않는다.** 대비 2.5:1로 크게 미달한다. 밝은 시안은 밑줄과 강조 면 바탕으로만 쓰고, 그 위 글자는 `#08333d`처럼 진한 색을 쓴다.

**`확인 필요`에 경고색을 쓰지 않는다.** 값이 없는 것은 나쁜 것이 아니라 확인되지 않은 것이다.

**본문 대비는 4.5:1, 큰 글자는 3:1을 넘긴다.** 2026-08-04 감사에서 `4.4:1`짜리가 20곳 나왔다. 원인은 보조 본문 색 하나였다.

## Typography

한 얼굴만 쓴다 — **Pretendard Variable.** 제품 UI는 제목용 서체를 따로 두지 않는다. 판정서 문법은 서체가 아니라 크기·굵기·괘선이 만든다.

### Scale

고정 크기를 쓴다. **`clamp()`로 제목을 화면 폭에 묶지 않는다** — 좁은 칸에서 오히려 나빠진다.

```
42px   판정 한 마디            굵기 800  자간 -0.03em
29px   근거 값                 굵기 800  자간 -0.02em
24px   구역 제목               굵기 700
20px   강조 본문 · 소제목       굵기 600
18px   본문 기준               굵기 400   ← 결정 대기: 17px과 렌더 비교 중
15px   라벨 · 배지             굵기 600~700
13px   눈금 축 숫자만          mono
```

**13px은 눈금 축에만 쓴다.** 읽지 않아도 판단이 되는 자리다. 그 밖의 모든 글자는 15px 이상으로 둔다.

자간 하한은 **-0.04em**이다. 그보다 좁으면 한글 자획이 붙는다.

### Weight

넷만 쓴다. **`550` `650` `720` `750` `760` 같은 임의값을 쓰지 않는다.**

```
400  본문
600  강조 본문 · 라벨
700  소제목 · 버튼
800  판정 한 마디 · 근거 값
```

**900은 쓰지 않는다.** Pretendard 900은 한글에서 자획이 뭉쳐 오히려 읽기 나빠진다. 크게 보이고 싶으면 크기를 키우고 굵기는 800에서 멈춘다.

### Line

본문 줄 간격 1.7, 제목 1.2. 산문은 한 줄 65~75자를 넘기지 않는다. 데이터 표는 더 촘촘해도 된다.

## State vocabulary

상태는 **색 + 부호 + 낱말** 셋으로 표시한다. 색만으로 판단하게 하지 않는다.

```
기준 안     ✓ 계열 부호   초록
주의        ! 계열 부호   황
기준 밖     × 계열 부호   적
확인 필요   ? 계열 부호   회색
```

부호는 **기하 부호를 쓰고 이모지를 쓰지 않는다.** 이모지는 플랫폼마다 모양이 달라지고 색·굵기를 화면 문법에 맞출 수 없다. 작물 선택의 작물 이모지는 예외로 남긴다 — 그건 상태가 아니라 대상 표시다.

### 기준 대비 눈금

값만 보여주지 않고 **기준에서 얼마나 벗어났는지 그림으로** 보여준다. 문제정의서 요구사항 ④가 요구하는 "전문 용어를 직관적인 시각 요소로 치환"이 이것이다.

- **연속값**(pH·기온): 가로 트랙에 기준 띠를 칠하고 내 값에 표식을 찍는다
- **등급값**(적성등급 1~5): 칸을 나눠 해당 칸까지 채운다
- **단계값**(위험 낮음·주의·높음): 세 칸

## Layout

- 터치 타깃은 **44×44 이상**. 2026-08-04 감사에서 체크박스가 14×14로 나왔다
- 카드를 기본값으로 쓰지 않는다. 카드 안에 카드를 넣지 않는다
- 1차원 배치는 flex, 2차원은 grid
- **색을 띠로 두른 사이드 스트라이프(`border-left: 3px`)를 쓰지 않는다.** 현재 9곳 있고 걷어낼 대상이다
- 반응형은 구조로 다룬다. 글자 크기를 화면 폭에 연동하지 않는다

## Motion

전환 150~250ms. 상태 변화·피드백·불러오기에만 쓴다. 장식으로 움직이게 하지 않는다.

`prefers-reduced-motion: reduce`에 대안을 반드시 둔다. 지도의 경계 확대는 애니메이션을 쓰지만, 프레임이 오지 않는 환경에서 중간에 멈춰도 판정과 표시에는 영향이 없다.

## 스타일시트를 다시 썼다 (2026-08-04)

토큰만 바꿔서는 화면이 바뀌지 않는 이유를 실측으로 확인한 뒤 `app/globals.css`를
처음부터 다시 썼다. 이전 파일의 전수 조사는 `docs/20260804_스타일시트_인벤토리_v1.md`에 있다.

| 항목 | 이전 | 지금 |
|---|---|---|
| 행수 | 4,169 | 3,186 (주석 포함) |
| 규칙 블록 | 766 | 397 |
| 두 번 이상 재선언된 선택자 | **237개** (최다 7회) | 값이 겹치는 선택자·속성 **8쌍** |
| `font-size` 값 종류 | **39종** | **7종**(토큰) + Leaflet 예외 1 |
| `font-weight` 값 종류 | **11종** | **4종** |
| `:root` 밖 색 리터럴 | **129곳** | 0 (지도 반투명 덮개 3곳은 `rgb()/알파`) |
| `clamp()` | 10 | 0 |
| `!important` | 10 | 9 (Leaflet 5 · 동작 축소 4) |
| `@media print` | 없음 | 있음 |
| 대비 미달 | 20곳(4.4:1) | **0곳** (02·03·04 전 화면 실측) |

핵심 원인 둘을 기록해 둔다.

**첫째, 화면의 실제 값이 파일 뒤쪽 `보정` 구역에서 나왔다.** 1957행 이후 2,213줄이
보정층이었고 앞쪽 원본 규칙은 대부분 덮여 있었다. 그래서 앞을 고쳐도 화면이 그대로였다.

**둘째, 지도 타일에 `sepia(0.08)` 필터가 걸려 있었다.** 화면에서 가장 큰 요소가 흙빛으로
물들어 있어서, 팔레트를 시안으로 옮긴 뒤에도 화면이 따뜻하게 보였다. 채도만 낮추고
색조는 건드리지 않도록 고쳤다.

## 기준 눈금 — 구현했다

값이 기준에서 얼마나 벗어났는지 그림으로 보여준다. 문제정의서 요구사항 ④가 요구하는
`전문 용어를 직관적인 시각 요소로 치환`이 이것이다.

```
pH 6.2      ────[ 기준 6.0–6.5 ]────┃──      연속값: 기준 띠 + 내 값 표식
3급지       ■■■□□                             등급값: 칸 채우기
보통        ■■□                               단계값: 세 칸
```

숫자는 **규칙 엔진이 `AnalysisFactor.meter`로 확정해 넘긴다.** 화면이 문자열을 되풀어
읽거나 값을 다시 계산하지 않는다. 판정에는 쓰지 않는 표시 전용 필드다.

축 계산은 `띠 폭의 절반(최소 한 눈금)`을 양쪽에 두고, 값이 그보다 멀면 한 눈금 더 밀어낸
뒤 눈금 단위로 맞춘다. 축을 띠에만 맞추면 벗어난 값이 잘리고, 값에만 맞추면 띠가 축을
거의 다 덮어 이탈 거리가 보이지 않는다.

붙이지 않은 자리와 그 이유도 적어 둔다.

- **오이 기온** — 낮 22–28℃ · 밤 15–18℃로 축이 둘이다. 한 눈금에 담을 수 없고 낮·밤을
  섞은 평균은 어느 공식 기준과도 대조되지 않는다. 비워 두는 것이 없는 기준을 그리는 것보다 옳다
- **EC · 유기물** — 대조할 작물별 공식 기준을 아직 확보하지 못했다
- **배수** — 행 문구는 V3 원문 6단계(`약간 불량`)를 보여주고 눈금은 위험 산식이 실제로 쓴
  3단계를 가리킨다. 물리성 자료가 없는 경로에는 코드가 없어 6칸 중 어디인지 정할 근거가
  없다. 두 어휘가 다른 것은 의도한 것이라 눈금 옆에 `점수에 반영된 단계`라고 적는다

실측 확인 — 대관령 294-7전 · 사과에서 pH 6.2가 6.0–6.5 띠 안 46.7% 자리에 찍히고,
기온 31.8℃가 15–18℃ 띠(축의 10%) 오른쪽 밖 72.8% 자리에 찍힌다. `tests/factorMeter.test.ts` 22건.

## 남은 작업

### 1. 인쇄 3쪽 — 절반만 됐다

`@media print`를 넣었다. 조작하는 것을 걷어내고, 어두운 면을 반전하고, 한 항목이 쪽
경계에서 끊기지 않게 하고, 링크 주소를 본문에 남긴다. **여기까지는 지금 열려 있는
화면에 대해 작동한다.**

**세 쪽을 한 문서로 뽑지는 못한다.** 이 앱은 `view` 상태에 따라 한 번에 한 화면만 DOM에
두기 때문에, 03을 보면서 인쇄하면 02·04는 애초에 문서에 없다. 세 쪽으로 만들려면 셋을
동시에 렌더한 뒤 화면에서만 하나씩 보이게 해야 하고, 그 변경은 `[hidden]`과 `display`가
얽힌 자리를 건드린다(TSX 419–424 주석 참고).

### 2. 03·04 배치 결정 (팀 확인 완료)

- **03 사용한 자료 표는 화면에 남긴다.** PDF로 보내면 "전부 공개한다"를 5분 안에 보여줄 자리가 사라진다. 피드백이 강점으로 꼽은 화면이다
- **04 쉬운 말 보고서는 인쇄물로 보낸다.** 긴 산문이고 들고 다닐 내용이다. 다만 **AI 배지는 화면에 남긴다**
- 수익 모델은 화면에 넣지 않는다. 발표에서 말로 한다

### 3. 마인드맵을 새 구조 안에서 짠다 (팀 결정)

`origin/ui-mindmap`은 동기가 02를 마인드맵으로 재구성한 갈래다(`6c60688`). 판정을 가운데
두고 근거를 가지로 펼치며, 누르면 값·기준·역할·출처가 나온다. 03 한 화면에 4,492자가
쌓여 있었다는 측정이 근거다.

**마크업과 상호작용 아이디어는 쓰고 스타일은 새 구조 안에서 다시 짠다.** 그 브랜치의 CSS는
옛 파일 기준으로 쓰였으므로 그대로 합칠 수 없다. 방사형 배치와 빈 공간 활용은 시안으로
먼저 본다. 그 사이 `ui-mindmap`에서 `globals.css`를 건드리지 않기로 했다.

### 4. 그 밖

- **본문 17px 대 18px** 확정. 지금 18px이고, 토큰 `--t-body` 하나만 바꾸면 전체가 따라온다
- 03 토양도 표에 밭·논·과수 **세 등급을 함께** 놓고 `판정 근거`를 표시. 지금은 작물이 보는
  등급 한 줄만 이름과 값이 맞게 나온다(2026-08-04 수정). 셋을 나란히 보여주는 것은 남은 일이다
- 눈금 색을 못 보는 사용자를 위한 확인. 채운 칸의 형태 차이가 필요한지 판단한다

### 이미 해결된 것 (기록만 남긴다)

- ~~하드코딩된 색 129곳~~ → 스타일시트 재작성으로 0
- ~~`clamp()` 10곳~~ → 0
- ~~14px 미만 56곳~~ → 눈금 축 13px과 지도 저작권 10px만 남았고 둘 다 의도한 예외
- ~~굵기 11가지~~ → 4가지
- ~~사이드 스트라이프 9곳~~ → 0. 자료 나이 표시의 왼쪽 괘선만 남겼다(색이 정보를 담는 자리)
- ~~체크박스 터치 타깃 14×14~~ → 24×24, 감싸는 `label`이 44×44
- ~~화면 문구 6곳의 `밭 적성등급` 고정~~ → 작물이 보는 등급으로 (`3323355`)
