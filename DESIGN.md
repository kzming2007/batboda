# Design

> 확정: 2026-08-04 본선 Day 1 야간
> 근거: 멘토 피드백(이미현), 문제정의서 요구사항 ④, 브라우저 접근성 감사 실측
> 전제: `PRODUCT.md`의 사용자(초보 귀농인 + 고령층)와 피할 방향(초록 클리셰 / 관공서 표 덩어리)

---

## Theme

밝은 테마 하나. 다크 모드는 두지 않는다.

물리적 장면이 이것을 정한다 — **처음 농사를 시작하려는 사람이 낮에 밭 앞이나 집 책상에서, 폰이나 노트북으로 확인한다.** 야외 햇빛과 고령층 가독성이 겹치므로 밝은 배경과 높은 대비가 필요하다. 어두운 테마는 이 장면에 맞지 않는다.

## Color

**전략은 Restrained.** 브랜드 색 하나와 상태색 넷만 쓴다. 장식으로 쓰는 색은 없다.

### 왜 초록을 버리는가

농사라서 초록을 쓰는 것은 분야만 듣고 짐작되는 첫 반사다. 그 다음 반사인 흙빛 테라코타·황토도 같은 이유로 피한다. **딥 시안은 두 반사 밖에 있고, 상태색 셋과 색조가 충분히 떨어져 혼동이 없다.**

초록은 버리지 않는다. **`기준 안` 상태에만** 남긴다. 상태에만 쓰이는 초록은 초록 일색이 아니다.

### Tokens

```
--pg          #f7f9fa   페이지 배경. 채도 거의 0, 브랜드 색조로만 살짝 기울임
--sf          #ffffff   카드·패널
--ink         #14181a   본문·제목
--ink-soft    #55636a   보조 본문. 배경 대비 5.6:1
--line        #dde4e6   괘선
--line-strong #b9c4c7   강조 괘선

--cy          #0e7490   브랜드 주 색. 주요 버튼 바탕(흰 글자 5.4:1)
--cy-ink      #0b5a72   브랜드 글자색. 링크·현재 선택 낱말
--cy-lt       #06b6d4   강조. 현재 선택 밑줄, 강조 면 바탕
--cy-pale     #cbf1f8   강조 면 바탕(진한 글자 얹음)

--good        #1f7a3d   기준 안
--good-bg     #dcecdf
--watch       #b5730a   주의
--watch-bg    #f7ead2
--bad         #c0392b   기준 밖
--bad-bg      #f8e2de
--unknown     #55636a   확인 필요 — 경고색을 쓰지 않는다
--unknown-bg  #e6eae7
```

### 지켜야 할 것

**밝은 시안 `#06b6d4`에 흰 글자를 얹지 않는다.** 대비 2.5:1로 크게 미달한다. 밝은 시안은 밑줄과 강조 면 바탕으로만 쓰고, 그 위 글자는 `#08333d`처럼 진한 색을 쓴다.

**`확인 필요`에 경고색을 쓰지 않는다.** 값이 없는 것은 나쁜 것이 아니라 확인되지 않은 것이다.

**본문 대비는 4.5:1, 큰 글자는 3:1을 넘긴다.** 2026-08-04 감사에서 `4.4:1`짜리가 20곳 나왔다. 원인은 보조 본문 색 하나였다.

## Typography

한 얼굴만 쓴다 — **Pretendard Variable.** 제품 UI는 제목용 서체를 따로 두지 않는다. 판정서 문법은 서체가 아니라 크기·굵기·괘선이 만든다.

### Scale

고정 크기를 쓴다. **`clamp()`로 제목을 화면 폭에 묶지 않는다** — 좁은 칸에서 오히려 나빠진다.

```
42px   판정 한 마디            굵기 800  자간 -0.03em
29px   근거 값                 굵기 800  자간 -0.02em
24px   구역 제목               굵기 700
20px   강조 본문 · 소제목       굵기 600
18px   본문 기준               굵기 400   ← 결정 대기: 17px과 렌더 비교 중
15px   라벨 · 배지             굵기 600~700
13px   눈금 축 숫자만          mono
```

**13px은 눈금 축에만 쓴다.** 읽지 않아도 판단이 되는 자리다. 그 밖의 모든 글자는 15px 이상으로 둔다.

자간 하한은 **-0.04em**이다. 그보다 좁으면 한글 자획이 붙는다.

### Weight

넷만 쓴다. **`550` `650` `720` `750` `760` 같은 임의값을 쓰지 않는다.**

```
400  본문
600  강조 본문 · 라벨
700  소제목 · 버튼
800  판정 한 마디 · 근거 값
```

**900은 쓰지 않는다.** Pretendard 900은 한글에서 자획이 뭉쳐 오히려 읽기 나빠진다. 크게 보이고 싶으면 크기를 키우고 굵기는 800에서 멈춘다.

### Line

본문 줄 간격 1.7, 제목 1.2. 산문은 한 줄 65~75자를 넘기지 않는다. 데이터 표는 더 촘촘해도 된다.

## State vocabulary

상태는 **색 + 부호 + 낱말** 셋으로 표시한다. 색만으로 판단하게 하지 않는다.

```
기준 안     ✓ 계열 부호   초록
주의        ! 계열 부호   황
기준 밖     × 계열 부호   적
확인 필요   ? 계열 부호   회색
```

부호는 **기하 부호를 쓰고 이모지를 쓰지 않는다.** 이모지는 플랫폼마다 모양이 달라지고 색·굵기를 화면 문법에 맞출 수 없다. 작물 선택의 작물 이모지는 예외로 남긴다 — 그건 상태가 아니라 대상 표시다.

### 기준 대비 눈금

값만 보여주지 않고 **기준에서 얼마나 벗어났는지 그림으로** 보여준다. 문제정의서 요구사항 ④가 요구하는 "전문 용어를 직관적인 시각 요소로 치환"이 이것이다.

- **연속값**(pH·기온): 가로 트랙에 기준 띠를 칠하고 내 값에 표식을 찍는다
- **등급값**(적성등급 1~5): 칸을 나눠 해당 칸까지 채운다
- **단계값**(위험 낮음·주의·높음): 세 칸

## Layout

- 터치 타깃은 **44×44 이상**. 2026-08-04 감사에서 체크박스가 14×14로 나왔다
- 카드를 기본값으로 쓰지 않는다. 카드 안에 카드를 넣지 않는다
- 1차원 배치는 flex, 2차원은 grid
- **색을 띠로 두른 사이드 스트라이프(`border-left: 3px`)를 쓰지 않는다.** 현재 9곳 있고 걷어낼 대상이다
- 반응형은 구조로 다룬다. 글자 크기를 화면 폭에 연동하지 않는다

## Motion

전환 150~250ms. 상태 변화·피드백·불러오기에만 쓴다. 장식으로 움직이게 하지 않는다.

`prefers-reduced-motion: reduce`에 대안을 반드시 둔다. 지도의 경계 확대는 애니메이션을 쓰지만, 프레임이 오지 않는 환경에서 중간에 멈춰도 판정과 표시에는 영향이 없다.

## 남은 작업

순서가 중요하다. 아래 1번을 하지 않으면 색을 판단할 수 없다.

### 1. 하드코딩된 색 105곳을 토큰으로 (최우선)

`app/globals.css`에 색 지정이 **133곳**인데 `:root` 토큰 안은 28곳뿐이다. **나머지 105곳은 값이 코드에 직접 박혀 있다.** `rgba()` 직접 지정도 32곳이다.

그래서 팔레트를 토큰에서 바꿔도 화면의 8할은 옛 색으로 남는다. 2026-08-04 실측이고, **새 팔레트도 옛 팔레트도 아닌 상태로 보이는 원인이 이것이다.** 기계적인 치환 작업이다.

### 2. 기준 눈금 구현

**아직 만들지 않았다.** 시안에만 있고 코드에는 없다. 값과 라벨만 있어서 여백이 비어 보이고 직관성이 떨어지는 실제 원인이다.

`grep`에 `track`·`band`·`steps`가 잡히지만 전부 다른 용도다.

```
pH 6.9   ━━━━[기준 6.0–6.5]━━┃━━━   연속값: 기준 띠 + 내 값 표식
2급지    ■■□□□                      등급값: 칸 채우기
주의     ■■□                        단계값: 세 칸
```

### 3. 03·04 배치 결정 (팀 확인 완료)

- **03 사용한 자료 표는 화면에 남긴다.** PDF로 보내면 "전부 공개한다"를 5분 안에 보여줄 자리가 사라진다. 피드백이 강점으로 꼽은 화면이다
- **04 쉬운 말 보고서는 인쇄물로 보낸다.** 긴 산문이고 들고 다닐 내용이다. 다만 **AI 배지는 화면에 남긴다**
- 수익 모델은 화면에 넣지 않는다. 발표에서 말로 한다

### 4. 인쇄 레이아웃 3쪽

화면을 그대로 인쇄하면 버튼·탭·지도가 종이에 나온다. `@media print`로 인쇄용 순서를 따로 짠다. 새 라이브러리는 넣지 않는다.

```
1쪽   판정 · 근거 요약 · 먼저 할 일
2쪽   사용한 자료 표
3쪽   쉬운 말 보고서
```

### 5. 그 밖

- 본문 17px 대 18px 실제 렌더 비교 후 확정 (지금 18px)
- `clamp()` 10곳 고정 크기로 교체
- 14px 미만 56곳 정리. 첫 화면에서 `내 농지` 라벨이 10px, 체크박스 터치 타깃이 14×14로 측정됐다
- 굵기 11가지를 4가지로 (`550` `650` `720` `750` `760` 임의값 제거)
- 사이드 스트라이프 9곳 제거
- **03 토양도 표에 밭·논·과수 세 등급과 `판정 근거` 표시.** 엔진은 이미 작물별 등급을 쓰는데 화면 문구 6곳이 `밭 적성등급`으로 고정돼 있다. 사과·배를 고르면 화면 문구가 실제 판정 근거와 어긋난다

## 진행 중인 갈래

`origin/ui-mindmap` — 동기가 02를 마인드맵으로 재구성했다(`6c60688`). 판정을 가운데 두고 근거 일곱을 가지로 펼치며, 누르면 값·기준·역할·출처가 나온다. 03 한 화면에 4,492자가 쌓여 있었다는 측정이 근거다. 03·04 코드는 남겨 뒀다.

`:root`를 건드리지 않아 팔레트 작업과 겹치지 않는다. 로컬에서 `origin/ui-mindmap` 위에 팔레트 커밋을 얹어 확인했고 충돌이 없었다.

**아직 채택하지 않았다.** 방사형 배치와 빈 공간 활용을 시안으로 더 보기로 했다.
