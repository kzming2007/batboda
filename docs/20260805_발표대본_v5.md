# 2026-08-05 본선 발표 대본 v5 — 9장 정렬 · 훅 오프닝판

> 발표: **2026-08-05 14:10~14:20** · 발표 순서 **1번** · 발표자 **남궁도현**
> 구성: 발표 5분(라이브 시연 70초 포함) + 질의응답 5분
> 슬라이드: **9장**
> 배점: 기획 30 · 구현 30 · 운영 가능성 25 · 프로젝트 발표 15

## 0. v4에서 무엇을 바꿨나

**사실 판단은 v4와 같다. 승계한 것은 그대로 두고, 앞머리와 장 수만 바꿨다.**

승계하는 것:

- **v3 0절 「쓰지 않는다」 표 전부.** 그 표를 먼저 읽고 이 파일로 온다.
- **v3 5절 질의응답 28문.** 이 파일에 옮기지 않았다. 무대에는 v3와 v5를 함께 띄운다. 두 벌로 두면 한쪽만 고쳐질 위험이 있다.
- **장별 금지 목록**은 `docs/20260805_발표자료_내용설계_v1.md`의 것을 각 구간에 다시 적었다.

바꾼 것은 넷이다.

| | v4 | v5 |
|---|---|---|
| 장 수 | 10장 | **9장.** 「다섯 고리」 단독 장을 없애고 2장 한 줄로 접었다 |
| 작물 전환 증거 | S5(2:10 지점) | **1장으로 올렸다.** 표지에서 바로 던진다 |
| 오프닝 | 문제 서술 → 서비스 소개 | **답할 수 없는 질문 → 화면의 사실 하나로 회수** |
| 말투 | 장마다 조금씩 달랐다 | **청중에게 던지는 질문은 `~을까요`, 사실을 말하는 문장은 `~입니다`로 통일** |

### 2차 멘토링을 어떻게 반영했나

멘토 지적은 **"임팩트를 빡 줘서 뭔가 하나가 있어야 한다"**, **"나열하면 안 들린다"**였다.

그래서 1장을 나열의 반대로 만들었다. 문제 설명·팀 소개·기능 목록을 앞에 두지 않고, **화면에 같은 농지 두 카드를 띄운 뒤 답할 수 없는 질문 하나를 던지고, 답이 화면에 있음을 보인다.**

멘토가 든 자기 예시(「계획 잘 지키는 사람 손 들어보세요」)는 청중 반응에 의존한다. 우리는 그 형태를 쓰지 않았다. **확인된 근거가 없는 통계를 쓸 수 없기 때문에**(귀농 인구·실패율·손해액·시장 규모 전부 금지) 반응을 유도할 재료가 없고, 손이 하나라도 올라가면 오프닝이 무너진다. 대신 **화면에 이미 있는 사실 하나**(같은 농지, 두 등급)로 같은 효과를 낸다.

### 팀원 차별점 문안 처리

| 문안 | 처리 |
|---|---|
| "단순한 추천이 아니라, 근거(수치·출처·기준시각)까지 눈으로 확인하게 만드는 정직한 시스템" | **9장 마무리에 넣었다.** 5장·6장의 주장을 한 문장으로 묶는 말이다 |
| "시·군 단위를 넘어, 귀농인이 딛고 선 단 한 칸의 필지" | **2장에 넣었다.** 다섯 고리를 한 낱말로 요약한다. 화면 낱말은 `농지`로 통일돼 있으므로 **`내 농지 한 칸`으로 한 번 이어 준다** |
| "평균치만 믿고 농사지었다가는 실패함" | **쓰지 않는다.** 금지 목록의 `실패 예방`에 걸리고, 확인된 근거가 없는 인과 주장이다. 순서 1번이라 "실패율 자료가 있습니까"가 뒤 팀들까지의 기준 질문이 된다 |
| "같은 면 안에서도 필지마다 등급과 산도가 완전히 다름" | **쓰지 않는다.** 필지 간 변이는 측정한 적이 없다. 무대에서 증명하는 것은 **같은 필지에서 작물을 바꾸면 등급이 갈린다**는 다른 주장이고, 섞으면 1장의 증거가 흐려진다 |
| 위 자리에 쓸 우리 실측 = `258필지 중 18건, 7.0%` | **8장에만 둔다.** 3장에 같은 숫자를 두 번 말하지 않는다 |

---

## 1. 시간 배분

| 장 | 설계 | 실측 예상 | 누적 | 주장 | 배점 |
|---|---:|---:|---:|---|---|
| 1 표지 겸 훅 | 25 | **30** | 0:30 | 같은 농지인데 작물을 바꾸면 공식 등급이 갈린다 | 기획 · 발표 |
| 2 단 한 칸 | 22 | 22 | 0:52 | 그 한 칸의 근거를 끝까지 되짚는다 (다섯 고리) | 기획 |
| 3 왜 안 이어지나 | 22 | 23 | 1:15 | 자료는 있고, 서로 다른 기관·단위에 있다 | 기획 |
| **4 라이브 시연** | **70** | **70** | 2:25 | 다섯 고리가 배포본에서 실제로 이어진다 | 구현 |
| 5 규칙과 AI | 35 | **39** | 3:04 | 판정은 규칙, AI는 옮겨 쓰기, 틀리면 되돌림 | 구현 |
| 6 안 될 때 | 30 | 30 | 3:34 | API가 죽어도 판단은 이어지고 상태를 구분한다 | 운영 |
| 7 누가 다시 오나 | 25 | 27 | 4:01 | 반복 사용은 매일이 아니라 사건마다 | 운영 |
| 8 못 하는 것 | 25 | 28 | 4:29 | 한계를 화면에 적고 검증 순서를 정했다 | 운영 · 발표 |
| 9 마무리 | 12 | 14 | 4:43 | 근거를 눈으로 확인하게 만드는 서비스 | 발표 |

**설계 합 266초 · 실측 예상 283초 · 여유 17초.** 여유는 라이브가 늦을 때 쓴다. 남으면 남긴다. 채우지 않는다.

- 실측 예상은 발표용 낭독 **초당 5.5음절** 기준으로 구간별 음절을 센 값이다.
- 초과 17초의 출처는 둘이다. **1장 +5초** — 작물 전환 초대 문장을 넣었다. **5장 +4초** — 검사 항목 세 개를 말로 풀고 `±20%`를 되살렸다. 나머지 8초는 7·8·9장이 각 2~3초씩 넘긴 것이다.
- **여유가 20초를 밑돈다.** 리허설이 **4분 50초**를 넘기면 이 순서로 뺀다. ① 7장 사용 상황 세 개를 두 개로 **−3초** ② 3장 「초보자는 이 네 화면을 각각 찾아…」 한 문장 **−6초** ③ 6장 세 상태 나열 **−5초**. 셋을 다 빼면 여유 31초가 된다.
- **줄이지 않는 곳** — 1장 오프닝과 초대 문장, 라이브 70초, 5장의 검사 항목·`±20%`·각주, 8장 분모, 9장.
- **실제로 넘기는 장은 8회다.** 4장이 70초 동안 배포본 화면에 머문다.

---

## 2. 5분 대본

읽는 대본이 아니다. **굵은 문장은 그대로 말하고, 나머지는 뜻만 지킨다.** 수치는 화면에 보이는 값을 읽는다. 외운 값을 말하지 않는다.

**말투 기준** — 청중에게 던지는 질문은 `~을까요`, 사실을 말하는 문장은 `~입니다`. 장마다 존댓말 등급을 바꾸지 않는다. `맞습니까`처럼 따져 묻는 소리를 내지 않는다. 사용자가 초보 귀농인과 고령층이고 제품이 지향하는 인상이 「친근하다·쉽다·편안하다」이므로, 말투도 같이 생각해 보자는 쪽에 둔다.

### 1장 · 0:00~0:30 · 같은 농지, 두 등급 (설계 25초 · 실측 30초)

**화면** — 이천 유방동 870답 한 필지. 사과 **과수 4급지** 카드와 상추 **밭 3급지** 카드가 나란히. 두 등급 숫자만 크게.

> 화면의 농지 하나입니다. 경기 이천 유방동 870답.
>
> *(한 박자)*
>
> **여기에 사과를 심는 게 맞을까요, 상추를 심는 게 맞을까요?**
>
> *(2초 침묵 — 답을 기다리지 않는다. 시선을 심사위원석에 두고 손은 화면을 가리키지 않는다)*
>
> 저희도 몰랐습니다.
>
> **공식 토양도는 사과에 과수 4급지, 상추에 밭 3급지를 씁니다.**
>
> **작물을 바꾸면 판정에 쓰는 공식 등급 자체가 갈립니다.**
>
> **다른 작물을 고르셔도 같은 방식으로 동작합니다. 질의응답에서 바로 열어 보이겠습니다.**
>
> 팀 김남궁택, 밭보다의 남궁도현입니다.

**이 장이 발표 전체의 승부처다.**

- **침묵은 질문 뒤 한 번만.** 2초를 세고 넘어간다. 3초를 넘기면 청중이 답해야 하는 자리로 오해한다.
- **`과수 4급지`와 `밭 3급지`는 또박또박 끊어 말한다.** 심사위원이 이 두 숫자를 읽어야 주장이 선다. 다른 문장보다 반 박자 느리게.
- **`저희도 몰랐습니다`를 빼지 않는다.** 질문을 던져 놓고 답을 아는 척하면 몰아붙이는 소리가 된다. 이 한마디가 질문을 같이 생각하는 자리로 바꾼다.
- **초대 문장을 빼지 않는다.** 이 장은 라이브가 아니라 캡처다. 캡처를 보인 직후가 **꾸민 화면이라는 의심이 생기는 자리**이므로, 같은 자리에서 열어 보이겠다고 못 박는다. 4장 시연 마무리에서 한 번 더 반복한다.
- 팀 소개는 **한 문장.** 인원·학과·역할·서비스 기능 목록을 말하지 않는다. 등급 두 개를 말한 다음에 이름을 대는 순서를 바꾸지 않는다 — 이름이 먼저 오면 훅이 죽는다.

**넣지 않는다** — 귀농 인구·실패율·손해액·시장 규모(확인된 근거 없음), 「평균치만 믿으면 실패한다」류의 인과 주장, 「같은 면 안에서도 필지마다 다르다」(측정한 적 없고 이 장의 주장과 다르다), 필드 코드명, 등급 네 종류 전수 설명, 청중에게 손을 들게 하는 유도.

### 2장 · 0:25~0:47 · 단 한 칸 (22초)

**화면** — 농지 경계 폴리곤 · 지번 · PNU. 아래에 다섯 고리 한 줄.

> 저희가 보는 단위는 시·군 평균이 아닙니다. **지금 딛고 선 단 한 칸의 필지입니다.** 화면의 낱말로는 **내 농지 한 칸**입니다.
>
> 밭보다는 **그 한 칸의 근거를 처음부터 끝까지 되짚습니다.**
>
> 좌표가 아니라 농지 경계를 먼저 확인하고, 같은 지번의 토양, 작물별 공식 등급, 그 좌표의 예보, 출처와 기준시각까지. **다섯 고리입니다.**

발표 순서 1번이므로 이 장이 **비교 기준어를 남기는 자리**다. 다섯 낱말을 끊어 말한다.
`단 한 칸의 필지`와 `내 농지 한 칸`을 이어 말한다. 말로는 `필지`를 써도 되지만 화면 낱말이 `농지`라 한 번은 붙여 준다.

**넣지 않는다** — API 개수, 아키텍처 도해 설명, 「필지 단위는 밭보다뿐」, 다섯 고리 아래 추가 설명(제목이 전부다), 다른 팀·기관의 부재 주장.

### 3장 · 0:47~1:10 · 왜 내 농지로 안 이어지나 (22초)

**화면** — 03 「사용한 자료」 표. 제공기관 열과 기준시각 열이 넷 다 다르다는 것만 보이면 된다.

> 그런데 자료가 없어서 막히는 게 아닙니다. **자료는 다 있습니다.**
>
> 화면의 표가 밭보다가 쓴 자료입니다. 네 줄을 다 읽지 않으셔도 됩니다. **넷이 서로 다른 기관, 서로 다른 단위, 서로 다른 기준시각입니다.**
>
> 초보자는 이 네 화면을 각각 찾아, 자기 농지에 해당하는 값을 골라, 작물 기준과 대조해야 합니다. **그 대조가 안 되는 것이 문제입니다.**

**표를 읽지 않는다.** 기관 이름 넷을 일일이 읊으면 22초를 넘긴다. 손으로 기준시각 열을 한 번 훑는 것으로 끝낸다. 기관 이름은 질문받으면 답한다(농림축산식품부 팜맵 · 농촌진흥청 · 기상청).

**넣지 않는다** — 「대부분의 서비스는 시·군 평균만 준다」, 기존 서비스 비교, 데이터 개수 자랑, 흙토람을 경쟁자로 부르기(우리가 쓰는 자료의 출처로만 부른다), `258필지 중 18건`(8장에서 말한다).

### 4장 · 1:10~2:20 · 라이브 시연 (70초)

**화면을 배포본으로 전환한다.** 슬라이드를 읽지 않는다. → **3절 큐시트를 그대로 실행한다.**

### 5장 · 2:20~2:54 · 판정은 규칙이 하고 AI는 옮겨 쓴다 (35초)

**화면** — 04 상단(결론 한 줄 + 「판정은 규칙이 확정했습니다」 병기) + 우측에 **검증 5단계**.

> 결론 문장은 AI가 썼습니다. **판정을 정한 것은 규칙입니다.**
>
> 화면의 다섯 단계가 그 경로입니다. **AI에는 규칙이 확정한 근거만 넘깁니다. 나온 문장이 판정이나 근거 밖의 숫자를 건드리면 화면에 나가지 못합니다.**
>
> 걸리면 같은 근거의 규칙 문장으로 되돌립니다. 과부하 때는 더 가벼운 모델로 자동 대체합니다.
>
> 회귀 테스트 13파일 115건을 통과했고, 감점 폭을 흔든 560개 시나리오에서 주 판정 단계는 바뀌지 않았습니다. **다만 코드 회귀와 산식 민감도 근거이고, 현장 정확도 검증은 아닙니다.**

**검증 5단계는 읽지 않는다.** 다섯을 다 읽으면 35초를 넘긴다. **위 두 굵은 문장이 다섯 단계의 요약이고, 나머지는 화면에 맡긴다.** 손으로 다섯 줄을 위에서 아래로 한 번 훑는다.

**마지막 각주 문장을 빼지 않는다.** 이 한 문장이 앞의 두 수치를 과장에서 근거로 바꾼다. 빼면 「115건 통과」가 품질 보증으로 들린다.

수치는 **당일 아침 재실행 결과**로 말한다. 다르면 다시 센 숫자를 말한다.

**넣지 않는다** — 모델 이름·버전(화면 배지가 실제 응답한 모델을 표시한다. 물으면 「과부하 때 더 가벼운 모델로 자동 대체하고 배지에 그대로 표시한다」까지만), 「AI가 모든 숫자 오류를 차단한다」, 검사 항목 다섯 개 나열, 프롬프트 전문, 560을 정확도로 제시, 위험 라벨 변동(완화 60% · 보수 10%는 질의응답용).

### 6장 · 2:54~3:24 · 안 될 때 (30초)

**화면** — 03 사용한 자료 표의 「상태 · 기준시각」 열 확대.

> 운영에서 중요한 것은 잘 될 때가 아니라 **안 될 때**입니다.
>
> 화면을 보시면 **소스마다 상태와 기준시각이 따로 붙어 있습니다.** 지금은 넷 다 실시간입니다. **실시간 연결, 저장해 둔 검증 스냅샷, 시연용 자료를 구분해 씁니다.** 실패한 소스만 대체하고, 나머지는 실시간을 그대로 씁니다.
>
> **8월 3일에 공공데이터 인증이 실제로 막혔습니다.** 그때 이 경로로 판정까지 이어지는 것을 확인했습니다. **가정이 아니라 겪은 일입니다.**

**「지금은 넷 다 실시간입니다」를 빼지 않는다.** 세 상태 구분은 우리 표시 규칙이고, 오늘 화면에 세 가지가 동시에 나오지는 않는다. 장애를 꾸며 캡처하지 않기로 했으므로 그 차이를 말로 메우는 자리다.

**넣지 않는다** — 운영비·운영 인력 주장, 지역 확대 건수, 발표장에서 장애 재현 시도, 조작해 만든 401 화면.

### 7장 · 3:24~3:51 · 누가 다시 오는가 (25초)

**화면** — 01 「내 농지」 즐겨찾기 목록.

> 이 서비스는 매일 쓰는 것이 아닙니다. **판단이 필요한 사건마다 옵니다.** 농지를 계약하기 전, 작물을 바꿀 때, 파종 전입니다.
>
> 그래서 농지를 즐겨찾기로 담아 둡니다. 브라우저에만 저장합니다.
>
> **재방문율은 아직 측정하지 않았고, 유료 고객도 없습니다.** 첫 실증은 귀농귀촌센터와 농업기술센터 상담 창구에 **제안하고**, 반복 사용이 확인되면 기관용 라이선스를 **검토하겠습니다.**

`제안`과 `검토`를 말로도 붙인다. 밀리면 사용 상황 세 개를 **두 개로 줄인다**(계약 전, 작물 바꿀 때).

**넣지 않는다** — 「센터가 매일 쓴다」, 기관 도입 확정, 시장 규모·라이선스 금액, 사계절 달력이나 업무 흐름 도해 설명.

### 8장 · 3:51~4:19 · 못 하는 것 (25초)

**화면** — 03 「최근 관측 기록」 + 「현재 판정 방식」.

> 못 하는 것을 화면에 적어 두었습니다. 세 가지입니다.
>
> 토양 산도는 대관령 반경 1킬로미터 **258필지 중 18건, 7.0퍼센트**만 조회됐습니다. **없는 값을 다른 농지에서 채우지 않습니다.**
>
> 예보는 3일 단기예보만 씁니다.
>
> 관측소가 **6.45킬로미터** 떨어져 있어 거리를 적고 **점수에 넣지 않습니다.**
>
> **현장 정확도는 아직 검증하지 않았습니다. 그래서 참고 판정입니다.**

**7.0퍼센트는 반드시 분모와 함께 말한다.** 이 숫자가 「내 농지의 값은 대개 비어 있다」는 우리 실측이고, 평균으로 채우지 않는 이유다.

**화면 문구를 그대로 읽지 않는다.** 화면은 세 줄로 있고 말은 위 문장으로 한다. 변명 톤을 섞지 않는다 — 사실만 말하고 넘어간다.

**한계를 네 개 이상 늘리지 않는다.** 중기예보 미구현·EC 미반영·작물 5종은 질의응답에서 답한다. 과수·논 등급은 한계가 아니다(1장의 강점이다).

### 9장 · 4:19~4:33 · 마무리 (12초)

**화면** — 제품명 + 농지 경계 한 장 + QR.

> **단순한 추천이 아니라, 판단의 근거를 눈으로 확인하게 만드는 서비스입니다.** 농지 경계부터 근거의 출처까지.
>
> QR로 지금 열어 보실 수 있습니다. **작물만 바꿔 보시면 근거가 갈립니다.**
>
> 감사합니다.

마지막 굵은 문장이 **1장의 질문을 회수한다.** 여기서 새 기능·수치·로드맵을 꺼내지 않는다. 멈추고 질의응답으로 넘어간다.

---

## 3. 라이브 시연 70초 큐시트

**전제** — 6절 아침 확인표를 통과한 경우에만 04까지 넣는다. 하나라도 실패하면 03에서 닫고 04를 언급하지 않는다.
**공통** — 후보 건수, 위험 라벨, 표시 모델, 기온 값은 **화면에 나온 값을 읽는다.** 대사에 박지 않는다.

| 구간 | 화면 동작 | 말 |
|---|---|---|
| 0~8초 | 01에서 지번 검색 → 후보에서 대관령면 85-61전 선택 | **"지금 배포된 서비스입니다. 다섯 고리가 실제로 이어지는지 보시겠습니다."** → **"지번과 경계를 사람이 확인합니다."** 후보에서 고르는 동작을 손으로 보인다 |
| 8~15초 | 상추 · 3일 선택 → 분석하기 | **"토양은 이 필지번호로, 예보는 이 좌표로 가져옵니다."** |
| 15~28초 | 02 판정서 상단 | **"판정과 함께, 어떤 근거가 좋음이고 어떤 근거가 주의인지, 네 갈래 공공데이터가 응답했는지를 같이 공개합니다."** 판정 낱말과 상태 배지를 화면에서 읽는다 |
| 28~42초 | 03 근거 화면 · 기온 줄과 산도 줄 | **"평균기온이 상추 공식 기준을 얼마나 벗어났는지 눈금으로 보입니다."** 기준 구간과 실측 위치를 손으로 가리킨다. 이어 출처표에서 **기관과 기준시각**을 짚는다 |
| 42~62초 | 04 쉬운 말 보고서 | **"04에서는 판정을 규칙이 정했다고 먼저 밝히고, AI가 근거와 먼저 할 일을 쉬운 말로 옮깁니다."** |
| 62~70초 | 04 하단 | **"이 자료로는 알 수 없어 현장에서 봐야 하는 것까지 같이 적습니다."** 5장으로 넘어간다 |

**1장에서 이미 작물 전환을 보였으므로 시연에서 작물을 바꾸지 않는다.** 재분석에 시간이 들고 5초 규칙에 걸릴 자리가 하나 늘어난다. **작물 전환은 질의응답에서 라이브로 연다**(4절).

**말하지 않는다** — 분석에 걸린 초, 후보 건수, 「어느 농지나 몇 초 안에」, 위험 라벨을 미리 정해 말하기, 등급과 저해요인을 한 원인처럼 합쳐 말하기.

### 3-1. 5초 규칙

**한 동작을 누르고 5초 안에 다음 화면이 나오지 않으면 기다리지 않는다.** 같은 동작을 반복하지 않는다. 재클릭·새로고침·네트워크 전환을 하지 않는다. **원인을 설명하지 않는다.**

### 3-2. 고정 전환 문장

> **"지금은 저장된 검증 장면으로 이어가며, 서비스에서는 실시간과 검증 스냅샷, 시연 자료를 구분해 표시합니다."**

이 문장을 그대로 말한다. **실시간인 척하지 않는다.** 이 한마디가 6장의 주장과 같은 말이라, 사고가 오히려 운영 근거가 된다.

### 3-3. 백업 순서

미리 분석해 둔 결과 탭 → 정적 캡처 **C1 → C2 → C3 → C4 → C7**. **로컬 서버는 켜지 않는다.** 발표 중 명령줄을 보이지 않는다.

### 3-4. 어디서 멈췄든 이어붙일 문장

| 멈춘 자리 | 이어붙일 말 |
|---|---|
| 01 후보 조회 | "경계 확인은 이 화면에서 사람이 합니다. 캡처로 보시겠습니다." |
| 경계가 그려지지 않음 | "식별자가 일치할 때만 경계를 그립니다. 없으면 그리지 않습니다." |
| 02 판정 | "판정과 근거를 같이 공개하는 화면입니다." |
| 03 근거 | "기준과 실측을 눈금으로 겹쳐 보이는 자리입니다." |
| 04 쉬운 말 | "판정은 규칙이 정했고 AI는 옮기기만 했다는 것을 여기서 밝힙니다." |
| AI 문장이 규칙 문장으로 떨어짐 | 숨기지 않는다. "AI 호출이 안 되어 같은 근거의 규칙 문장으로 내려갔습니다. 화면에 그 사실이 적혀 있습니다." |
| 네트워크 전면 차단 | 정적 캡처로. 전환 문장 후 나머지를 슬라이드로 진행 |

---

## 4. 질의응답 태도

**v3 5절 28문을 쓴다.** 이 파일에 옮기지 않았다. 자주 나올 순서로 다섯만 적어 둔다.

1. 흙토람과 무엇이 다릅니까 → **경계 확인부터 근거 추적까지 한 흐름에서 작물 기준과 대조하는 것**
2. 정확한가요 → **현장 정확도는 검증하지 않았습니다. 참고 판정입니다**
3. AI가 틀리면요 → **검사에 걸리면 규칙 문장으로 되돌립니다**
4. 다른 지역도 되나요 → **행정구역별 정적 DB를 새로 만들지 않는 구조입니다**
5. 수익은요 → **유료 고객과 확정 가격이 없습니다. 첫 실증을 상담 창구로 제안합니다**

### 4-1. 화면을 열어 답할 것 하나

**질문이 판정 논리나 차별성으로 오면 유방동 870답을 라이브로 연다.** 작물만 사과 ↔ 상추로 바꿔 등급이 갈리는 것을 그 자리에서 보인다. 1장에서 카드로 보인 것을 실물로 회수하는 자리다. 5초 규칙은 여기서도 적용한다.

### 4-2. 모를 때 쓸 문장 — 이 셋으로 굳힌다

멘토 지적: **"모르면 당당하게, 그 부분까지는 생각하지 못했지만 고려해서 추후에 반영해보겠습니다."**

> ① **"그건 아직 측정하지 않았습니다. 지금 말씀드리면 추정이 됩니다."**
> ② **"그 부분까지는 생각하지 못했습니다. 고려해서 다음 검증에 반영하겠습니다."**
> ③ (숫자가 기억나지 않을 때) **"수치를 정확히 기억하지 못해 지금 말씀드리지 않겠습니다. 화면과 문서에 실측 기록이 있어 바로 확인해 드릴 수 있습니다."**

- **①과 ②를 구분한다.** ①은 측정 계획은 있었으나 값이 없는 것, ②는 아예 생각하지 못한 것이다. ②를 ①처럼 말하면 변명으로 들리고, ①을 ②처럼 말하면 준비가 없어 보인다.
- **사과를 반복하지 않는다.** "죄송합니다"는 질문을 못 들었을 때만 쓴다.
- **말끝을 흐리지 않는다.** 위 문장을 말한 뒤 바로 멈추고 다음 질문을 받는다. 뒤에 변명을 붙이면 모른다는 사실보다 태도가 더 남는다.
- **다른 팀·서비스 평가를 요구받으면** "다른 팀의 구현은 확인하지 못했고, 평가하지 않겠습니다. 저희가 증명할 수 있는 부분만 말씀드리겠습니다."

---

## 5. 「잘하는 건 내세우고 못 한 건 숨긴다」 — 우리 판단

멘토 조언과 8장이 부딪히는 것은 사실이다. **그래도 밝히는 쪽을 유지한다.**

근거는 셋이다. 첫째, **먼저 밝히면 신뢰가 되고 나중에 들키면 실점이 된다.** 우리 한계는 화면에 이미 적혀 있어 심사위원이 QR로 열면 바로 보인다. 숨긴 채로 질의응답에 들어가면 같은 사실을 방어 자세로 말하게 된다. 둘째, **예선 피드백이 이미 이 태도를 강점으로 지목했다** — pH 7%와 560 시나리오를 숨기지 않은 점. 검증된 반응을 뒤집을 이유가 없다. 셋째, **1번 발표라 뒤 다섯 팀의 기준이 된다.** 한계를 말한 팀 다음에 말하지 않은 팀이 오면 비교가 우리에게 유리하다.

다만 멘토 조언을 **분량과 위치로** 받는다.

- **한계는 세 개로 제한한다.** 네 개를 넘기면 나열이 되고, 멘토가 지적한 「안 들리는 발표」가 된다. 넷째부터는 물으면 답한다.
- **한계 장을 앞에 두지 않는다.** 8장, 즉 강점을 다 보인 뒤다. 4·5·6장에서 232초 중 135초를 강점에 쓰고 한계에 28초를 쓴다.
- **한계를 강점으로 포장하지도 않는다.** 「없으면 없다고 적는다」까지만 말하고 자랑으로 넘기지 않는다.
- **한계가 아닌 것을 한계로 말하지 않는다.** 과수·논 등급은 1장의 강점이다. v3·v4에서 이미 한계 목록에서 뺐다.

---

## 6. 발표 직전 확인

### 6-1. 아침에 다시 잴 것

- [ ] 회귀 테스트 재실행 → **파일 수·건수를 5장 대사에 맞게 고친다**
- [ ] 배포본에서 대관령면 85-61전 · 상추 · 3일을 **3회 연속** 성공시킨다
- [ ] **유방동 870답에서 사과 ↔ 상추 전환을 1회 확인한다** — 1장 카드와 질의응답 라이브가 같은 값이어야 한다
- [ ] 02·03·04의 평균기온이 **모두 같은 값**인지 본다
- [ ] 03 「초보자 설명」이 `연결됨`인지 본다

### 6-2. 노트북

- [ ] 알림·업데이트·절전 차단. 휴대전화 무음 후 뒤집어 둠
- [ ] 화면 복제로 고정. 브라우저 배율을 캡처 때와 같게
- [ ] 탭 순서 — ① 배포본(입력 화면) ② 미리 분석해 둔 결과 ③ 유방동 870답 결과 ④ 캡처 폴더
- [ ] 개인 계정·확장 아이콘이 보이지 않게
- [ ] 주력 탭에서 시연 조합을 한 번 끝까지 돌려 캐시를 데운 뒤 01로 되돌린다

### 6-3. 슬라이드와 리허설

- [ ] **9장인지 확인한다.** 10장짜리 구버전이 열려 있지 않게
- [ ] 1장에 두 등급 카드가 나란히 있고 **`과수 4급지`·`밭 3급지` 숫자가 뒷줄에서 읽히는지** 실제로 뒷줄에 서서 확인
- [ ] 3장 표는 기준시각 열이 넷 다 다르게 보이는지만 확인. 글자가 작아도 된다
- [ ] 발표자 도구로 노트가 보이는지 확인 — 각 장 노트에 초와 금지 목록
- [ ] **1장만 3회 소리 내어 연습한다.** 25초 안에 들어오고 침묵이 2초인지 잰다
- [ ] 전체 리허설 2회. 각 회가 **4분 45초 안**에 끝나야 안전하다

---

## 7. 근거

- 슬라이드별 주장·근거·금지: `docs/20260805_발표자료_내용설계_v1.md`
- 사실 감사와 질의응답 28문, 「쓰지 않는다」 표: `docs/20260805_발표대본_v3.md`
- 직전 대본과 구간 나누기: `docs/20260805_발표대본_v4.md`
- 수치 원천: `docs/20260804_야간_작업분담_v1.md` 「검증된 사실」
- 운영 가능성 문안: `docs/20260804_운영_가능성_정리_v1.md`
- 작물 용도별 등급과 유방동 870답 사례: `docs/20260804_적성등급_적용_감사_v1.md`
- 예선 피드백(한계 공개를 강점으로 지목): `docs/20260804_계정이전_UI작업_기록_v1.md` 1-1
