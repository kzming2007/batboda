# 본선 인계 — 노트북에서 여기부터 읽으세요

> 작성: 2026-08-03 (김택명 PC) · 대상: 본선 현장 노트북
> 본선: **2026-08-04(화) 13:00 ~ 08-05(수) 18:00 · 베르누이 호텔 · 1박 2일**
> 이 문서만 읽고 이어갈 수 있게 썼습니다. 새 대화를 시작하면 이 파일을 먼저 읽히세요.

## 지금 막혀 있는 것 — 도착하면 이것부터

**2026-08-04 갱신: Gemini 유료 전환 완료. 로컬은 정상입니다. 남은 것은 Vercel 환경변수 반영뿐입니다** → [3-1절](#3-1-gemini-유료-전환--로컬-완료--vercel-반영-남음)

유료 전환 과정에서 프로젝트가 새로 생기면서 `gemini-2.5-flash`가 **404로 막혔습니다**(신규 프로젝트 차단). 모델을 `gemini-3.5-flash`로 옮겨 해결했습니다. 배포본이 아직 옛 키·옛 모델을 쓰고 있어 `AI를 불렀지만 응답을 받지 못해 규칙 문장으로 바꿨습니다`가 계속 뜹니다.

---

## 0. 노트북 첫 30분

### 0-1. 저장소 받기

```bash
git clone https://github.com/kzming2007/batboda.git
cd batboda/prototype/codex-mvp
npm ci
```

Node.js **20.9.0 이상** 필요. 김택명 PC는 Node 24.18.0 / npm 11.16.0에서 확인했습니다.

### 0-2. `.env.local` 만들기 — **이건 저장소에 없습니다**

`prototype/codex-mvp/.env.local` 을 손으로 만듭니다. 5줄입니다.

```dotenv
PUBLIC_DATA_MODE=live
DATA_GO_KR_SERVICE_KEY=<공공데이터 일반 인증키 Decoding>
LLM_PROVIDER=replay-live
GEMINI_API_KEY=<AI Studio 키>
GEMINI_MODEL=gemini-3.5-flash
```

**키 두 개는 Vercel 대시보드에서 가져오세요.** Settings → Environment Variables → 눈 아이콘.
`.gitignore`가 `.env*`와 `env.local`을 막고 있어 저장소에는 절대 올라가지 않습니다.

**공공데이터 키는 반드시 `Decoding` 쪽입니다.** `%2B`·`%3D`가 보이면 Encoding이라 잘못된 값입니다. 코드가 URL 인코딩을 직접 하므로 이미 인코딩된 값을 넣으면 이중 인코딩되어 `SERVICE_KEY_IS_NOT_REGISTERED_ERROR`가 납니다. 실측으로 확인했습니다.

### 0-3. 실행

```bash
npm.cmd run dev
```

**PowerShell에서 `npm run dev`가 `UnauthorizedAccess`로 막히면** 둘 중 하나:
- `npm.cmd run dev` 로 우회
- 또는 한 번만 `Set-ExecutionPolicy -Scope CurrentUser RemoteSigned` 실행 후 `Y`

### 0-4. 동작 확인 (5분)

`http://localhost:3000` 에서:

1. 하단에 **`공공데이터 연결 준비됨`** 이 뜨는지 → 안 뜨면 `DATA_GO_KR_SERVICE_KEY` 문제
2. **`실제 공공데이터로 조회`** 체크
3. `평창 대관령` → `주변 농지 찾기` → 지번 `85-61` 검색 → 필지 선택 → `상추` → `3일` → 분석
4. **`실시간 연결 4/4`** 와 판정 `조건부 적합` 이 나오면 정상. 로컬 실측 **2.2~2.7초**

### 0-5. 검증 명령

```bash
npm run test    # 65개 통과해야 정상
npm run lint
npm run build
```

---

## 1. 지금 어디까지 왔는가

### 배포

| 항목 | 값 |
|---|---|
| 배포 URL | **https://batboda.vercel.app** |
| Vercel 프로젝트 | `kzming2007/batboda` (Hobby) |
| Root Directory | **`prototype/codex-mvp`** ← 재설정 시 반드시 이 값 |
| 함수 리전 | `icn1`(서울) — `vercel.json`에 고정. 정부 API가 전부 국내라 필수 |
| 배포본 응답 시간 | 분석 **약 2.5초** (로컬과 동일. 리전 설정이 먹은 증거) |

### 저장소

| 원격 | 주소 | 용도 |
|---|---|---|
| `origin` | `kzming2007/batboda` | **작업용.** push하면 Vercel 자동 배포 |
| `upstream` | `ngdh0309/batboda` | 남궁도현 원본. `git fetch upstream && git merge upstream/main` |

**2026-08-03 시점 모든 커밋이 push 완료**되어 있습니다(0 ahead / 0 behind).

### 검증된 사실 (발표에 쓸 수 있는 실측값)

- **실시간 연결 4/4** — 팜맵·토양(2종)·단기예보·농업기상 전부 `connected`
- **대관령 후보 356건**, 반경 1km
- **토양검정 커버리지 7.0%** — 반경 1km 고유 PNU 258건 중 pH 조회 가능 18건
- **지역 확장 실증** — 김제 757건(밭 591·논 114·시설 49·과수 3), 나주 281건. **둘 다 실시간 성공.** 대관령 전용이 아님
- **시연 조합** — `대관령면 85-61전 + 상추 + 3일` → `조건부 적합`, pH 6.9, 2급지, 신뢰 100
- **테스트 65개** 통과, production build 통과
- **민감도 감사** — 560 합성 시나리오에서 감점 폭 ±20% 변화 시 주판정 변동 **0%**. 위험 라벨은 완화 60%·보수 10% 변동(안정성 주장은 주판정에만 해당)

---

## 2. 2026-08-03에 한 일 (커밋 10건)

| 커밋 | 내용 |
|---|---|
| `949f291` | **Vercel 배포 설정** — 서울 리전 고정, `analyze`·`farm`에 `maxDuration = 60` |
| `57b1cef` | **공공데이터 오류에 거부 사유 표시** — 이 기능으로 `코드 30`(키 오류)을 찾아냈습니다 |
| `02185c4` | **AI 마크다운 제거** — Gemini가 `**조건부 적합**`을 써서 판정서에 별표가 노출됨. 02·04 양쪽 차단 |
| `c320246` | **pH 없는 필지에도 04 리포트 생성 + 논 안내** |
| `4cacfcb` | **위험 등급 인용 검사** — 판정만 검사해서 위험 등급은 바꿔 써도 통과했음. 검사 항목 수는 5개 유지 |
| `8d092fa` | **`replay-live` 모드** — 저장본 우선, 없으면 실시간 |
| `32a12d8` | **저장본을 수집 시점 근거로 검사** |
| `bc29ce9` | **04 실패 사유 화면 표시** |
| `56f177e` | **호출 실패와 검사 불통과 구분** |

### 주요 변경의 배경

**pH 없는 필지 (커버리지 대응)** — 검정 이력이 7.0%뿐이라 대관령 밖에서 04가 안내문 한 줄로 끝났습니다. 이제 밭 적성등급이 있으면 리포트를 만들고 **산도 문단만** `조회하지 못했습니다`로 적습니다. 없는 값을 채우지 않는 원칙은 그대로입니다. 등급까지 없으면 여전히 만들지 않습니다.

**논 안내** — 판정 근거인 `밭 적성등급`은 밭 기준입니다. 팜맵이 `논`으로 판독한 필지에 그대로 쓰면 근거가 약해지므로, 판정을 막지 않고 02 판정서에 한 줄 안내를 붙였습니다. 김제는 후보의 15%가 논이라 실제로 자주 뜹니다.

**`replay-live`** — `replay`는 저장된 조합만 답하고, `gemini`는 한도가 소진되면 아무 조합도 답하지 못합니다. 둘을 합쳐 시연 조합은 저장본으로 고정하고 나머지는 실시간으로 답합니다.

**저장본 근거 고정** — 저장본은 수집 시점 예보 수치를 문장에 담고 있어서, 오늘 근거로 숫자를 대조하면 예보가 갱신된 만큼 매번 검사에 걸렸습니다(실제로 `근거에 없는 숫자 '23.3'`으로 떨어짐). 수집할 때 그 시점의 판정·위험 등급·허용 숫자를 함께 저장하고 그 값으로 대조합니다. **판정이 바뀌었으면 저장본을 쓰지 않습니다** — 화면 머리글과 설명이 다른 결론을 말하면 안 되기 때문입니다.

---

## 3. 미해결 — 도착하면 바로 처리

### 3-1. Gemini 유료 전환 — 로컬 완료 · Vercel 반영 남음

**2026-08-04에 유료 전환을 마쳤습니다.** 하루 20건 제한은 사라졌습니다. 다만 전환 과정에서 두 가지가 걸렸고 둘 다 해결했습니다.

**걸린 것 ① — 옛 모델이 막혔습니다.** 결제 계정을 붙이며 프로젝트가 새로 생기자 `gemini-2.5-flash`가 이렇게 거부됩니다.

```
404 · This model models/gemini-2.5-flash is no longer available to new users.
```

**걸린 것 ② — 최신 모델은 느립니다.** 사고 과정을 끌 수 있는지가 갈림길입니다. 분석 1회가 호출 2건이라 이 차이가 그대로 대기 시간이 됩니다. 2026-08-04 실측:

| 모델 | 결과 |
|---|---|
| `gemini-2.5-flash` | **404 · 신규 프로젝트 차단** |
| **`gemini-3.5-flash`** | **1436ms · `thinkingBudget: 0` 정상 · 코드 수정 불필요** ← 채택 |
| `gemini-3.1-flash-lite` | 1241ms 정상. 더 빠르지만 지시 준수력이 낮아 예비 |
| `gemini-3.6-flash` | 4595ms. `thinkingBudget: 0`·`thinkingLevel` 둘 다 거부, 사고 토큰 517 |
| `gemini-3-flash-preview` | 23765ms |
| `gemini-flash-latest` | 400. 별칭이 3.6으로 풀림 — **별칭을 쓰지 마세요** |

**`GEMINI_MODEL=gemini-3.5-flash`로 바꿔 해결했습니다.** 로컬에서 `대관령면 85-61전 + 상추 + 3일`이 `조건부 적합` / `실시간 연결 4/4` / 02·04 **검사 5항목 전부 통과** / 배지 `AI 설명 · Gemini 3.5 Flash · 검사 통과`로 확인됐습니다.

**남은 일 — Vercel 대시보드에서 (직접 하셔야 합니다)**

1. Settings → Environment Variables → `GEMINI_API_KEY`를 **새 유료 키**로 교체
2. 같은 화면에서 `GEMINI_MODEL`을 **`gemini-3.5-flash`**로 교체 ← **이걸 빠뜨리면 배포본은 계속 404입니다**
3. Deployments → 맨 위 → `⋯` → **Redeploy** (7절 함정 ①)

이 셋을 끝내기 전까지 배포본은 `AI를 불렀지만 응답을 받지 못해 규칙 문장으로 바꿨습니다`를 계속 표시합니다.

### 3-2. 시연 조합 스냅샷 재수집

현재 저장본 3건에는 `capturedAt`이 없어 **재생되지 않습니다**(의도된 안전 동작 — 건너뛰고 실시간으로 넘어갑니다). 다시 수집해야 살아납니다.

**개발 서버에서만 됩니다**(운영 빌드는 403).

```
http://localhost:3000/api/report/capture?lat=37.675&lng=128.718&cropId=lettuce&horizonDays=3&parcelId=5176038024100850061
```

캡처 1회가 호출 2건을 씁니다. **일일 한도가 남아 있어야 성공합니다** — 3-1절 참조. 소진된 상태에서는 몇 분을 기다려도 실패합니다.

응답에서 **`ok: true`와 `validation.ok: true`를 확인**하세요. 통과한 문장만 저장 가치가 있습니다.

시연할 조합 3~5개를 정해 각각 한 번씩. 끝나면 `git push`로 배포본에 반영.

**유료 전환 후에도 이 작업은 하세요.** 돈이 아니라 **호텔 네트워크** 대비입니다.

### 3-3. Vercel `LLM_PROVIDER` 확인

`replay-live` 로 되어 있어야 합니다. **환경변수를 바꾸면 반드시 Redeploy** — 기존 배포본에는 반영되지 않습니다. 여기서 여러 번 헷갈렸습니다.

값이 `replay`면 저장본 없는 조합이 전부 `AI 생성 아님`이 됩니다.

### 3-4. 배포본 최종 확인 3건

- 판정서 첫 문장에 `**` 별표가 없는지 (확인 완료, 재확인만)
- 대관령 **294번대** 필지에서 04 리포트가 나오는지 (pH 없는 필지)
- 지도를 **김제**로 옮겨 논 안내가 뜨는지 ← **화면에서 눈으로 확인한 적 없음**

---

## 4. 본선 배점이 바뀌었습니다

| 항목 | 예선 | 본선 | 평가 기준 |
|---|---|---|---|
| 기획력 | 30 | 30 | 기획 의도가 명확·실질적인가 / 문제정의서 부합 / 차별화 |
| 구현 | 30 | 30 | 기획 요소를 **서비스 결과물로 구현**했는가 / 핵심 기능이 **데모·시연 가능한 수준**인가 |
| AI 활용 | **15** | **없음** | — |
| 프로젝트 발표 | 없음 | **15** | 발표 내용이 충실·전달력 있는가 / **문제·해결 방식·구현 결과·기대효과**를 명확히 설명 |
| 운영 가능성 | 25 | 25 | **지역·기업 현장에서 운영될 가능성** / **실제 사용자와 후속 고도화 방향이 구체적인가** |

### 세 가지 함의

1. **AI 활용 15점이 사라졌습니다.** 규칙·LLM 분리와 5항목 검사는 우리 최강 자산인데 그 자체로는 점수가 없습니다. **버리지 말고 `구현`·`운영 가능성`의 언어로 옮기세요.**

   > AI가 틀린 문장을 만들면 검사가 걸러내고 규칙 문장으로 되돌아갑니다. 그래서 사람이 지켜보지 않아도 현장에서 돌릴 수 있습니다.

2. **`구현`이 실물 평가입니다.** 배포 URL은 확보했습니다. 심사위원에게 **QR로 주세요.**

3. **`운영 가능성` 기준이 날카로워졌습니다.** `실제 사용자`와 `운영 주체`에 답이 없습니다 — 아래 5절.

### 멘토링 항목 (본선 제공)

- Claude를 활용한 서비스 구현
- **서비스 웹 배포 및 데모 구성** ← 이미 완료. 멘토 시간을 다른 데 쓸 수 있습니다
- 발표 자료 및 발표 방식

**멘토 시간은 초반에 쓰세요.** 배포는 끝났으니 **운영 가능성 관점의 피드백**을 받는 게 이득입니다.

---

## 5. 본선 작업 우선순위

상세 계획은 [`docs/20260802_본선_전략과_구현계획_v1.md`](docs/20260802_본선_전략과_구현계획_v1.md)에 있습니다. 아래는 8/3 진행분을 반영한 갱신판입니다.

### 이미 끝난 것 (계획서의 Day 1 항목)

- 웹 배포 ✓
- 커버리지 대응 (A)(pH 없이 판정) ✓ · (C)(화면에 드러내기) 부분 ✓
- 지역 확장 실증 ✓ (김제·나주 실시간 성공)

### Day 1 (8/4) 남은 것

| 순위 | 항목 | 근거 |
|---|---|---|
| ~~1~~ | ~~**Gemini 유료 전환**~~ | **완료(로컬).** Vercel 환경변수 2개 교체 + Redeploy만 남음 → 3-1절 |
| 2 | **시연 조합 스냅샷 재수집** | 네트워크 대비. `85-61전+상추+3일` 1건 완료, 나머지 조합 남음 |
| 3 | **운영 주체 시나리오 확정** | 운영 가능성 25점의 핵심. 아래 표에서 하나 고르고 `누가·언제·무엇을 대체하는가`를 한 문장으로 |
| 4 | **현장 사용자 테스트** | 멘토 2명·다른 팀 3명에게 배포 URL 주고 5분씩 관찰. `어디서 멈췄는가`를 그대로 기록 → `실제 사용자` 근거 |
| 5 | **실기기 모바일 확인** | 농민은 폰으로 봅니다. 375px 시뮬레이터는 통과했지만 실기기는 다름 |
| 6 | 커버리지 대응 (B) — 인접 필지 참고값 | pH 없는 필지에 `250m 떨어진 ○○의 검정값은 pH 6.9입니다. 이 필지의 값이 아닙니다` **판정 점수에는 절대 넣지 않음** |

**운영 주체 후보**

| 주체 | 근거 | 우리가 줄 수 있는 것 |
|---|---|---|
| 지자체 농업기술센터 | 이미 귀농 상담 창구 운영 | 상담 전 사전 진단, 상담 시간 단축 |
| 귀농귀촌종합센터 | 예비 귀농인이 처음 만나는 곳 | 지역·작물 선택 단계 자료 |
| 문제정의 기업 | 문제를 낸 당사자 | 자사 서비스에 판정 모듈 제공 |

### Day 2 (8/5) — 오전 마감, 오후 발표

**오전에 새 기능을 시작하지 마세요.** 어제 발견한 것 반영과 문구 수정만.

여유가 있으면: 결과 공유(판정서를 링크·이미지로) → 작물 추가(공식 기준 장부 확보한 것만) → 필지 경계 폴리곤.

**발표 구성 (15점)** — 소개서를 그대로 읽으면 안 됩니다. 평가 기준의 네 덩어리로:

| 구간 | 내용 |
|---|---|
| 문제 | 초보 귀농인은 판단 근거를 모을 수 없다. 자료는 흩어져 있고 코드값이다 |
| 해결 방식 | 필지 단위로 공공데이터를 모아 공식 기준과 대조. 판정은 규칙이, 설명은 AI가 |
| 구현 결과 | **배포 URL 라이브 시연** + QR 배포 |
| 기대효과 | 운영 주체 시나리오 + 사용자 테스트 결과 + 고도화 로드맵 |

**라이브 시연 + 녹화 백업 둘 다 준비하세요.**

### 예상 질문과 답의 뼈대

| 질문 | 답 |
|---|---|
| 커버리지가 낮은데 실제로 쓸 수 있나 | 화면에 표시하고, pH가 없어도 밭 적성등급으로 판정한다. 인접 참고값은 출처를 구분해 보조 표시 |
| AI가 틀린 말을 하면 | 다섯 항목으로 검사하고 걸리면 규칙 문장으로 되돌린다. **실제로 걸러낸 사례가 화면에 남아 있다** |
| 매번 문장이 달라지지 않나 | 판정과 수치는 규칙이 고정한다. 표현만 바뀐다 |
| 외부 API가 죽으면 | 실패한 소스만 검증 스냅샷으로 대체하고, 실시간이 아니라는 사실과 실패 이유를 화면에 적는다 |
| 누가 운영하나 | (Day 1에 확정) |
| **정확도는 검증됐나** | **검증되지 않았다.** 구현 민감도 감사는 했지만 현장 정확도 평가는 하지 않았다. 그래서 참고 판단으로만 제시하고 현장 확인을 대체하지 않는다고 화면에 적었다 |

**마지막 답을 정직하게 하세요.** 정확하다고 말하면 근거를 대야 하고, 없으면 무너집니다.

---

## 6. 하지 말 것

- **판정 산식 재설계** — 560 시나리오 감사를 통과한 `v4-stage-first`를 흔들지 않습니다
- **디자인 언어 교체** — A안(에디토리얼 판정서)으로 확정. 다시 바꾸면 이틀이 사라집니다
- **근거 없는 작물 추가** — 공식 기준 장부를 확보한 작물만
- **정확도 주장** — 현장 검증이 없습니다
- **중기예보 신규 연결** — 신청·승인 시간 필요
- **`최근 추이를 반영해 판정한다`고 말하기** — 화면에는 보여주지만 판정 점수에 넣지 않습니다. 구현과 어긋납니다
- **AI가 어디를 강조할지 정하게 하기** — 강조 색은 규칙이 고릅니다

---

## 7. 알려진 함정

| 함정 | 증상 | 대응 |
|---|---|---|
| **환경변수 변경 후 Redeploy 안 함** | 값을 바꿨는데 안 바뀜 | Deployments → 맨 위 → `⋯` → Redeploy |
| **Root Directory 미설정** | Vercel 빌드가 `package.json` 못 찾음 | `prototype/codex-mvp` |
| **`npm run dev`가 PowerShell에서 막힘** | `UnauthorizedAccess` | `npm.cmd run dev` |
| **옛 서버가 포트를 잡고 있음** | 설정을 바꿨는데 옛 동작 | `pkill`이 안 들을 때가 있습니다. **새 포트로 띄워 확인**하세요. 이걸로 두 번 헤맸습니다 |
| **Encoding 키를 넣음** | `SERVICE_KEY_IS_NOT_REGISTERED_ERROR` 코드 30 | Decoding 키 사용 |
| **Gemini 모델 404** | `no longer available to new users` | 유료 전환으로 프로젝트가 바뀌면 옛 모델이 막힙니다. `GEMINI_MODEL=gemini-3.5-flash` |
| **모델 별칭 사용** | 갑자기 400 또는 4초 이상 지연 | `gemini-flash-latest` 같은 별칭은 어느 날 3.6으로 풀립니다. **버전을 못박으세요** |
| **`.env.local`이 없음** | `시연용 검증 자료로 보는 중` | 0-2절 참조. **저장소에 없습니다** |

---

## 8. 코드 구조 요약

```
prototype/codex-mvp/
├─ app/api/            analyze · farm · soil · weather · status
│                      cache/capture · report/capture  (개발 전용, 운영 403)
├─ lib/public-data/    client.ts(6개 API 호출) · geo.ts(좌표 변환) · request.ts
├─ lib/analysis/       engine.ts(판정) · cropProfiles.ts(공식 기준+출처)
│                      soilCodes.ts(토양도 코드표) · modelPolicy.ts(자체 설계 계수)
├─ lib/report/         bundle.ts(근거 묶음) · contract.ts(프롬프트+5항목 검사)
│                      providers/ (gemini · replay · replay-live · curated · tampered · anthropic)
│                      showcase.ts(04 규칙 문장) · showcaseRunner.ts(04 경로 선택)
│                      snapshots/ (실호출 응답 저장본)
├─ lib/cache/          verifiedSnapshot.ts + snapshots/verified.json (공공데이터 스냅샷)
├─ components/         FarmDecisionApp.tsx(4페이지) · FarmMap.tsx
└─ tests/              10개 파일 65개
```

### `LLM_PROVIDER` 값

| 값 | 동작 |
|---|---|
| `none`(기본) | 규칙 문장만 |
| `gemini` | 항상 실시간 호출 |
| `replay` | 저장본만. 없으면 규칙 문장 |
| **`replay-live`** | **저장본 우선, 없으면 실시간. 배포 기본값** |
| `curated` | 사람이 쓴 문안. AI 아님을 화면에 명시 |
| `tampered` | 규칙 위반 문장 주입. **검사가 막는지 발표에서 보여줄 수 있습니다** |

`tampered`는 발표용 카드입니다. AI가 판정을 바꾸려 할 때 어떻게 막히는지 실시간으로 시연할 수 있습니다.

### 두 겹 스냅샷

| 계층 | 파일 | 덮는 것 |
|---|---|---|
| 공공데이터 | `lib/cache/snapshots/verified.json` | 팜맵·토양·예보·관측 |
| LLM | `lib/report/snapshots/llmResponses.json` | 02·04 설명 문장 |

**둘이 겹치면 시연 조합은 네트워크가 끊겨도 끝까지 돕니다.** 실패한 소스는 `검증 스냅샷` 배지로 표시되고 분석은 완료됩니다. 발표에서 그대로 말할 수 있는 내용입니다.

---

## 9. 문서 색인

| 주제 | 문서 |
|---|---|
| **본선 전략과 계획** | [`docs/20260802_본선_전략과_구현계획_v1.md`](docs/20260802_본선_전략과_구현계획_v1.md) |
| 예선 기준 통합 문서 | [`docs/RISE_AI_해커톤_예선_준비_통합.md`](docs/RISE_AI_해커톤_예선_준비_통합.md) |
| 소개서 검토 (Gemini 반영) | [`docs/20260726_소개서_PDF_검토_v2_Gemini반영.md`](docs/20260726_소개서_PDF_검토_v2_Gemini반영.md) |
| 소개서 5·9·11장 초안 | [`docs/20260726_소개서_5_9_11장_초안_v1.md`](docs/20260726_소개서_5_9_11장_초안_v1.md) |
| 핵심 요구사항 감사·시연 조합 | [`docs/20260725_핵심요구사항_감사와_시연조합_v1.md`](docs/20260725_핵심요구사항_감사와_시연조합_v1.md) |
| 실시간 검증·커버리지 | [`docs/20260725_2차PC_환경재현과_실시간검증_v1.md`](docs/20260725_2차PC_환경재현과_실시간검증_v1.md) |
| 화면 4페이지 구조 | [`docs/20260725_화면_4페이지_분리와_강조_v1.md`](docs/20260725_화면_4페이지_분리와_강조_v1.md) |
| 작물 공식 기준 장부 | [`docs/20260723_5개_작물_공식_기준_장부_v1.md`](docs/20260723_5개_작물_공식_기준_장부_v1.md) |
| 산식 민감도 감사 | [`docs/20260724_적합도_엔진_민감도_감사.md`](docs/20260724_적합도_엔진_민감도_감사.md) |
| 앱 상세 설명 | [`prototype/codex-mvp/README.md`](prototype/codex-mvp/README.md) |

---

## 10. 새 대화를 시작할 때

노트북에서 Claude에 이렇게 말하면 됩니다.

> `D:\...\batboda\HANDOVER.md` 를 읽고 현재 상황을 파악해 줘. 본선 8/4~8/5이고, 지금 우선순위는 (그때 정한 것)이야.

작업 원칙은 이 저장소의 `docs/RISE_AI_해커톤_예선_준비_통합.md`와 예선 CLAUDE.md 규칙을 따릅니다. 핵심만 옮기면:

- 공식 자료·팀 결정·AI 제안·미확인 사항을 구분해 적습니다
- 규칙 기반 계산과 LLM 설명을 분리합니다. **LLM이 적합도 수치나 농업 기준을 만들게 하지 않습니다**
- 실시간·캐시·시연용 자료를 화면과 문서에 구분해 표시합니다
- 과장 표현과 확인되지 않은 효과 수치를 쓰지 않습니다
- **API Key·token을 문서·프롬프트·코드에 기록하지 않습니다**
- 첫 결과를 완성본으로 두지 않습니다. 오류 화면·수정 지시·개선 결과를 기록합니다

---

## 11. 팀 확인 대기 항목

- **운영 주체** 세 후보 중 하나 (25점 직결)
- Gemini **유료 전환** 여부와 시점
- 시연할 조합 3~5개 (스냅샷 수집 대상)
- 발표에서 `tampered` 모드 시연을 넣을지
- 사전 사용자 테스트 가능 여부
