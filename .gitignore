# 인증키·환경변수 — 어떤 경우에도 커밋하지 않는다
.env
.env.*
env.local
env.*.local
*.pem

# 의존성·빌드 산출물
node_modules/
.next/
out/
build/
coverage/

# 임시·백업
tmp/
*.zip
.DS_Store
Thumbs.db

# 개인 작업 환경 설정과 외부에서 가져온 스킬 원본. 저장소에 넣지 않는다.
# 디자인 판단의 근거는 PRODUCT.md와 DESIGN.md에 남기므로 스킬 파일 자체는 필요하지 않다.
.claude/
발표/node_modules/

# Office 임시 잠금 파일. 문서를 열어 두면 생기고 닫으면 사라진다.
~$*
